ACCESS CONTROL

The system implements role-based access control (RBAC).
Administrators have full access to all modules.
Regular users can only read their own data.
External users have no access to the system.
